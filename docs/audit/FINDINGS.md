# SDL3 Audit Finding Registry

This registry assigns stable, never-recycled IDs. A suspicion is not a confirmed defect: candidates move from `suspected` to `investigating`, then to either `confirmed` or `dismissed`. Dismissed candidates remain documented in the relevant subsystem notebook.

## Stable ID classes

- `BUG-###`: functional or reliability defect.
- `SEC-###`: security or untrusted-input defect.
- `REL-###`: build, packaging, release, deployment, or portability defect.
- `IMP-###`: suggested improvement, kept separate from defects.

## Severity and prioritization

Defect severities follow the approved design:

- **Critical:** remotely exploitable, major security-boundary failure, or widespread unrecoverable corruption.
- **High:** crash, memory corruption, serious multiplayer desynchronization, or a shipped platform rendered unusable.
- **Medium:** incorrect gameplay, broken edge case, practically significant resource leak, or meaningful portability/release defect.
- **Low:** limited incorrect behavior, weak diagnostics, or minor robustness issue.

Improvements are ranked separately by expected benefit, implementation effort, and regression risk. They are not assigned defect severity without defect evidence. The `Severity/Priority` cell for an improvement records that evidence-backed priority profile.

## Registry

| ID | State | Severity/Priority | Confidence | Gate | Summary | Evidence |
|---|---|---|---|---|---|---|
| IMP-001 | confirmed | Medium benefit / Low effort / Low risk | High | Task 3 | Make the six no-argument server functions use strict C prototypes in declarations and definitions | [Static disposition](subsystems/01-server-protocol.md#task-2-candidate-dispositions) |
| IMP-002 | confirmed | Medium benefit / Low effort / Low risk | High | Task 3 | Make seven server length/index comparisons type-consistent; their reviewed bounds do not establish a defect | [Static disposition](subsystems/01-server-protocol.md#task-2-candidate-dispositions) |
| IMP-003 | confirmed | Low benefit / Low effort / Low risk | High | Task 3 | Mark or remove six intentionally unused GLib callback/signal parameters | [Static disposition](subsystems/01-server-protocol.md#task-2-candidate-dispositions) |
| IMP-004 | confirmed | Medium benefit / Low effort / Low risk | High | Task 3 | Remove dead `today`; restore intended state use for `was_playing` while fixing BUG-005 | [Static disposition](subsystems/01-server-protocol.md#task-2-candidate-dispositions) |
| BUG-001 | confirmed | Medium | High | Tasks 2 and 7 (quantity and reachability corrected in Task 7 Fix Round 1) | `TextureEx` dereferences failed surface loads before checking (both null-deref orderings reproduced under UBSan; `shaderstuff.h:67` is the asset-reachable one, `:55` additionally requires an `SDL_CreateSurface` failure since production callers pass `candyModif.sfc`), leaks two heap `SDL_Rect` per `LoadEmptyAndApply` — at most one such call runs per `InitCandy` because `mainmenu.cpp:204/214/224/228` is an `if`/`else if` chain, so 2 leaked rects, not 6 — and reassigns surfaces without destroying the previous ones | [Task 7 confirmed findings](subsystems/05-render-audio.md#confirmed-findings) |
| BUG-002 | confirmed | Medium | High | Task 3 | The installed SIGTERM handler calls logging, allocation, DNS/HTTP, socket, and exit routines outside the POSIX async-signal-safe set; runtime stress was omitted by user direction | [Static disposition](subsystems/01-server-protocol.md#task-2-candidate-dispositions) |
| SEC-001 | confirmed | High | High | Task 3 | The daemon ignores `setgid`/`setuid` failures and can continue with unintended privileges; fault injection was omitted by user direction | [Static disposition](subsystems/01-server-protocol.md#task-2-candidate-dispositions) |
| SEC-002 | confirmed | High | High | Task 3 | Untrusted plaintext master-response `Content-Length` reaches signed overflow and allocation sizes, allowing startup service termination; exact analyzer OOB wording and runtime reproduction are not claimed | [Length trace](subsystems/01-server-protocol.md#network-derived-length-and-index-trace) |
| SEC-003 | confirmed | High | High | Task 4 | Peer-controlled bubble coordinates and numeric fields reach unchecked board indexing and exception-throwing option parsing | [Peer-message proof](subsystems/02-network-client-sync.md#peer-messages-and-round-flow) |
| REL-001 | confirmed | Low | High | Task 3 | Server OOM diagnostics pass `size_t` to the signed `%zd` conversion | [Static disposition](subsystems/01-server-protocol.md#task-2-candidate-dispositions) |
| REL-002 | confirmed | Medium | High | Task 3; remediation design recorded in Task 9 | Both room-cap harnesses daemonize onto fixed ports, can test unrelated listeners, and tear down only the launcher; the tools harness also binds UDP 1511. Task 9 discharged its remediation ownership without changing test code: bind port 0, read the bound port back from the child, run it in the foreground as a direct child, assert it is alive after readiness, and terminate/wait in a `finally`; the same change removes `tools/server_tests/test_room_caps.py`'s UDP 1511 bind. The fix only becomes observable once IMP-016 makes CI run the suite | [Harness boundary](subsystems/01-server-protocol.md#build-operations-and-harness-boundary), [Task 9 remediation](subsystems/07-build-release-tooling.md#extensions-to-existing-entries) |
| IMP-005 | confirmed | High benefit / Medium effort / Medium risk | High | Tasks 2, 5, 6, and 7 (render slice reopened and re-closed in Task 7 Fix Round 1) | Default-initialize state-bearing C++ members; the reviewed network/gameplay construction paths do not establish a defect, Task 6 promoted its one reachable use-before-initialization to BUG-034, and Task 7's render slice — first closed as having none — was corrected in Fix Round 1: `TTFText::coords` is reachable and consequential through `BubbleGame::targetingText` and was promoted to BUG-043; the remaining `TextureEx`/`TTFText` members still have no reachable use-before-initialization | [Gameplay construction proof](subsystems/03-gameplay.md#reload-reset-and-construction), [Task 7 slice](subsystems/05-render-audio.md#confirmed-findings) |
| IMP-006 | confirmed | Medium benefit / High effort / Medium risk | High | Tasks 2, 5, and 7 (render slice closed) | Make bounded gameplay/render numeric conversions and integer-division intent explicit; neither the Task 5 nor the Task 7 slice has a proven precision defect | [Gameplay numeric disposition](subsystems/03-gameplay.md#reload-reset-and-construction), [Task 7 triage](subsystems/05-render-audio.md#static-review) |
| IMP-007 | confirmed | Medium benefit / Low effort / Low risk | High | Tasks 2 and 7 (copy-semantics half corrected in Task 7 Fix Round 2) | Replace `TTFText`'s silent no-op copy assignment, reset-to-empty copy constructor, and borrowed `curText` with explicit ownership, and fix the shared `Dispose()`/explicit-destructor singleton pattern that leaves `ptrInstance` dangling (`AudioMixer`, `TransitionManager`, `HighscoreManager`, `GameSettings`); the `Dispose()` half is never tripped by the current call order, but the copy half **is** — `levelsetScores` trips it, promoted to BUG-045 | [Task 7 confirmed findings](subsystems/05-render-audio.md#confirmed-findings) |
| IMP-008 | confirmed | Low benefit / High effort / Low risk | High | Tasks 2 and 9 (closed in Task 9) | Apply constness, API, cast, shadowing, and portability cleanup selectively. Task 9 closed the last open slice: neither analyzer inspects CMake/YAML/shell/Python/Nix/Markdown, so **0** diagnostics of any IMP-008 family have a path among Task 9's 21 files, and the twelve non-`SEC-003` `bugprone-unchecked-string-to-number-conversion` sites all live in files closed gates already dispositioned (`src/networkclient.cpp`, `src/bubblegame_net.cpp`, `src/mainmenu_netpanel.cpp`, `src/mainmenu_input.cpp`, `src/socket_compat.h`, `src/logger.h`, `src/roundstats_color.h`, `server/stats.c`, `server/ws.h`, `server/win32_compat.h`). What Task 9 did own — the vendored dependency boundary Task 2 deferred — produced IMP-023 and REL-014. No defect was promoted from any IMP-008 family | [Task 9 IMP-008 closure](subsystems/07-build-release-tooling.md#imp-008-closure), [Task 8 slice closure](subsystems/06-platform-ports.md#confirmed-findings) |
| IMP-009 | confirmed | Medium benefit / Medium effort / Low risk | High | Tasks 2 and 5; remaining owners later gates | Simplify redundant branches, dead stores, unused attachment helpers, and missing-default control flow after semantic review | [Gameplay analyzer disposition](subsystems/03-gameplay.md#reload-reset-and-construction) |
| IMP-010 | confirmed | Medium benefit / Medium effort / Medium risk | High | Tasks 3 and 7 (cross-owner disposition complete) | Handle raw allocation and asset-load failure consistently: server unchecked/`exit` mixes (Task 3), render's unchecked `precalc_cos`/`precalc_sin`, `plasma.raw` `exit(1)`, warn-then-crash `TextureEx` (defect side is BUG-001), warn-and-continue texture loads, silent-null text, and audio's permanent negative caching each follow a different policy | [Task 7 ownership table](subsystems/05-render-audio.md#static-review), [server ownership review](subsystems/01-server-protocol.md#allocation-owners-and-destruction-paths) |
| BUG-003 | confirmed | High | High | Task 3 | Playing-room kick and post-start whole-room closure can leave live fds in priority mode with no game; their next binary line reaches fatal `exit(EXIT_FAILURE)` | [Lifecycle proof](subsystems/01-server-protocol.md#authorization-and-room-lifecycle) |
| BUG-004 | confirmed | Medium | High | Task 3 | The documented/configurable upload admission limit is inert because `amount_transmitted` is initialized/read/reset but never incremented | [Static review](subsystems/01-server-protocol.md#build-operations-and-harness-boundary) |
| BUG-005 | confirmed | Medium | High | Task 3 | A sole survivor receives a win when the playing room reaches one player and another win when that final player later leaves | [Lifecycle proof](subsystems/01-server-protocol.md#authorization-and-room-lifecycle) |
| BUG-006 | confirmed | Medium | High | Task 3 | WebSocket upgrade assumes one `recv()` contains the full HTTP header; a legal fragmented header is consumed and then misclassified as plain TCP | [Length trace](subsystems/01-server-protocol.md#network-derived-length-and-index-trace) |
| BUG-007 | confirmed | High | High | Task 3 | Direct blocking/single-send output has no queue or deadline, and WebSocket relay reports every nonnegative short frame as a complete payload | [Final candidate disposition](subsystems/01-server-protocol.md#candidates) |
| BUG-008 | confirmed | Low | High | Task 3 | Creator-led whole-room closure frees the game but leaks every surviving seat's independently owned room nickname | [Ownership proof](subsystems/01-server-protocol.md#allocation-owners-and-destruction-paths) |
| BUG-009 | confirmed | Low | High | Task 3 | Empty nickname validation succeeds, allowing `CREATE ` to allocate an empty room that known LIST parsers cannot enumerate | [Length trace](subsystems/01-server-protocol.md#network-derived-length-and-index-trace) |
| SEC-004 | confirmed | High | High | Task 3; downstream impact in Tasks 4 and 6 | The server does not bind chat or binary sender identity to the connection's assigned room seat, allowing player/leader impersonation and hostile downstream fields; Task 6 confirmed the lobby consumes any `OPTIONS:` push unconditionally, host included, with no creator check | [Authorization review](subsystems/01-server-protocol.md#authorization-and-room-lifecycle), [lobby consumption](subsystems/04-lobby-settings-input.md#option-serialization-and-validation-step-2) |
| SEC-005 | confirmed | High | High | Task 3 | A 128-byte LAN probe overwrites every zero in `msg[128]`, after which `strstr()` can read beyond the stack buffer | [Length trace](subsystems/01-server-protocol.md#network-derived-length-and-index-trace) |
| SEC-006 | confirmed | Medium | High | Task 3 | Arbitrarily long peer digit runs execute unchecked signed overflow in `charstar_to_int()` before protocol/cap validation | [Length trace](subsystems/01-server-protocol.md#network-derived-length-and-index-trace) |
| REL-003 | confirmed | High | High | Tasks 3-4 and 8 (platform-guard side closed in Task 8); remediation/build boundary in Task 9 | Windows server/client code narrows `SOCKET`; the server also exceeds default `fd_set` capacity, while the client never enables nonblocking mode before per-frame receive. Task 8 closed the guard side: `CMakeLists.txt:38` excludes `server/` on `WIN32 OR MINGW OR EMSCRIPTEN`, so this project's build can never produce the Windows server `server/win32_compat.h` exists to support; only the Windows client is built, linking `ws2_32` (`:155-157`), so the client-side `MSG_DONTWAIT` no-op and missing `FIONBIO` are the whole shipped Windows socket surface | [Client extension](subsystems/02-network-client-sync.md#lobby-response-and-reachability-handling), [Task 8 extensions](subsystems/06-platform-ports.md#confirmed-findings) |
| REL-004 | confirmed | Medium | High | Tasks 3, 8 and 9 (client version added in Task 8; packaging closed in Task 9, severity Low → Medium) | Version metadata is hand-maintained and unlinked from the tag. **Five** distinct strings exist across build inputs: `2.2.1` (`server/CMakeLists.txt:28`, printed at `server/fb-server.c:43` and sent as the master-server user agent at `server/net.c:1160`), `2.4.9` (`server/net.c:1098` startup log), `v2.4.26` (`platform.h:23` `APP_VERSION`, rendered at `mainmenu_panels.cpp:471`), `2.4.27` (`android/app/build.gradle:15` `versionName` and the off-tag fallback at `build.yml:128`/`:233`), and `0.1.0` (`default.nix:22`); four reach a runtime or artifact surface. Task 9 added the two packaging consequences: `versionCode` is the literal `10` and no workflow step overrides it (measured: zero `versionCode`/`versionName` occurrences in `build.yml`), so two tagged releases produce APKs Android refuses to treat as upgrades independently of REL-007's certificate rotation; and `share/icons/fb.rc` holds only an `ICON` statement with **no** `VERSIONINFO`, so the shipped `.exe` carries no version resource while the NSIS installer around it does | [Build boundary](subsystems/01-server-protocol.md#build-operations-and-harness-boundary), [Task 8 extensions](subsystems/06-platform-ports.md#confirmed-findings), [Task 9 version trace](subsystems/07-build-release-tooling.md#release-version-signing-and-artifact-flow-step-3) |
| BUG-010 | confirmed | Low | High | Task 3 | A same-name claimant can terminate a still-connected open peer once its cached whole-second receive-activity delta reaches two | [Final candidate disposition](subsystems/01-server-protocol.md#candidates) |
| BUG-011 | confirmed | Medium | High | Task 3 | Repeated `START` resets readiness while peers remain priority; renewed acknowledgement removes already-priority fds from the replacement polling list | [Final candidate disposition](subsystems/01-server-protocol.md#candidates) |
| IMP-011 | confirmed | Medium benefit / Medium effort / Low risk | High | Task 3 | Replace the synchronous 50 ms accept-time WebSocket sniff with event-loop upgrade state | [Final candidate disposition](subsystems/01-server-protocol.md#candidates) |
| BUG-012 | dismissed | - | High | Task 4 | Priority ingress diverts `FB/` text to the command parser, whose `PART` branch removes the player and priority membership; the ID remains reserved | [Dismissed candidate](subsystems/02-network-client-sync.md#dismissed-candidates) |
| BUG-013 | confirmed | Medium | High | Task 4 | Disconnect/part paths leak `currentGame` and retain native partial input or WASM sync/session state that can contaminate reconnection | [Connection lifecycle](subsystems/02-network-client-sync.md#connection-and-room-lifecycle) |
| BUG-014 | confirmed | Medium | High | Task 4 | WASM round 2+ waits for 40 entries in `messageQueue` after routing all sync traffic into `syncQueue`, forcing the five-second timeout | [Round-flow proof](subsystems/02-network-client-sync.md#peer-messages-and-round-flow) |
| BUG-015 | confirmed | Medium | High | Task 4 | Native and WASM command state machines omit ordinary rejection types and do not reliably correlate success with the pending create/join operation | [Response handling](subsystems/02-network-client-sync.md#lobby-response-and-reachability-handling) |
| BUG-016 | confirmed | Low | High | Task 4 | Native reachability treats writable nonblocking connect as success without checking `SO_ERROR`, so refused endpoints can appear online | [Response handling](subsystems/02-network-client-sync.md#lobby-response-and-reachability-handling) |
| BUG-017 | confirmed | Medium | High | Task 4 | Native TCP setup searches each greeting `recv` independently and outbound paths accept short sends as complete, violating stream semantics | [Stream-semantics proof](subsystems/02-network-client-sync.md#native-tcp-stream-semantics) |
| BUG-018 | confirmed | Medium | High | Task 5 | Multiplayer board clear ignores the Clear Mode flag and ends Classic and Team Mode rounds | [Winner-state proof](subsystems/03-gameplay.md#round-winner-departure-and-match-transitions) |
| BUG-019 | confirmed | Medium | High | Task 5 | Sequential final-loss handling can credit a winner or both players before resolving a simultaneous draw | [Simultaneous-loss proof](subsystems/03-gameplay.md#round-winner-departure-and-match-transitions) |
| BUG-020 | confirmed | High | High | Task 5 | Distinct-match startup retains malus and transient round state; after player-count shrink a stale malus can index an inactive cleared board | [Reset-path proof](subsystems/03-gameplay.md#reload-reset-and-construction) |
| BUG-021 | confirmed | Medium | High | Tasks 5-6 (origin resolved in Task 6) | Disconnect handling bypasses configured continuation, one-team-survivor, and victories-limit semantics; Task 6 proved the origin: `CONTINUEGAMEWHENPLAYERSLEAVE` is edited and transmitted but has no `SetupSettings` field | [Departure-state proof](subsystems/03-gameplay.md#round-winner-departure-and-match-transitions), [option table](subsystems/04-lobby-settings-input.md#option-serialization-and-validation-step-2) |
| BUG-022 | confirmed | Medium | High | Task 5 | Chain target reservation uses standard parity on flipped grids and omits the reference cross-chain validity pass | [Chain-reaction proof](subsystems/03-gameplay.md#placement-collision-grouping-and-compression) |
| BUG-023 | confirmed | Medium | High | Tasks 5-6 (extended in Task 6) | The local two-player victories limit is neither propagated into gameplay nor enforced; Task 6 further proved the panel that edits it is unreachable (`showing2PPanel` is never set true, `SetupNewGame(2)` is never called) | [Local match proof](subsystems/03-gameplay.md#round-winner-departure-and-match-transitions), [transition map](subsystems/04-lobby-settings-input.md#menu-and-room-state-transitions-step-1) |
| BUG-024 | confirmed | Medium | High | Task 5 | Remote clear-win accounting depends on whether `F` is queued before deferred replicated-stick resolution | [Clear-order proof](subsystems/03-gameplay.md#round-winner-departure-and-match-transitions) |
| BUG-025 | confirmed | Medium | High | Task 5 Fix Round 1 | Maximum native delta can move a launched bubble completely through an occupied bubble between endpoint-only collision samples, changing its eventual attachment cell | [Maximum-delta proof](subsystems/03-gameplay.md#maximum-delta-collision-trace) |
| BUG-026 | confirmed | High | High | Task 6 | `ReadSettings` retries `CreateDefaultSettings`/`iniparser_load` in an unbounded loop, so an unwritable or unrepairable preferences file hangs startup before any window appears | [Persistence matrix](subsystems/04-lobby-settings-input.md#dynamic-evidence) |
| BUG-027 | confirmed | Medium | High | Task 6 | Any INI syntax error or over-long line makes the loader silently truncate the file and rewrite defaults, discarding every stored preference | [Persistence matrix](subsystems/04-lobby-settings-input.md#dynamic-evidence) |
| BUG-028 | confirmed | Medium | High | Task 6 | Key bindings are cast from unvalidated INI integers to `SDL_Scancode`; a stored 99999 survives into `PlayerKeys`, and `IsKeyPressed` then indexes SDL's 512-entry keyboard state | [Persistence matrix](subsystems/04-lobby-settings-input.md#dynamic-evidence) |
| BUG-029 | confirmed | Low | High | Task 6 | The `WindowHeight` upper-bound guard tests `windowWidth`, so out-of-range window heights are accepted verbatim | [Persistence matrix](subsystems/04-lobby-settings-input.md#dynamic-evidence) |
| BUG-030 | confirmed | Medium | High | Task 6 | A NaN `SpeedMultiplier` passes both ordered clamps and propagates into `deltaScale` and every per-frame movement | [Persistence matrix](subsystems/04-lobby-settings-input.md#dynamic-evidence) |
| BUG-031 | confirmed | Low | High | Task 6 | Settings-write failures are reported only through `SDL_LogWarn` on category 1, which SDL3 suppresses by default, and `CreateDefaultSettings`'s own `rval` error branch is unreachable | [Persistence matrix](subsystems/04-lobby-settings-input.md#dynamic-evidence) |
| BUG-032 | confirmed | High | High | Task 6 | Highscore and level-history files are parsed with `stoi`/`stof` without exception handling, so a corrupt file aborts the client during construction | [Full-client runs](subsystems/04-lobby-settings-input.md#dynamic-evidence) |
| BUG-033 | confirmed | Medium | High | Tasks 6 and 8 (platform dimension closed in Task 8) | Hosting a LAN server runs `system("pkill -x fb-server")`, terminating every `fb-server` process the user owns, not only an orphan of this client. Task 8 bounded the blast radius: the call is reachable only on macOS and Linux, because `StartLocalServer`/`StopLocalServer` are diagnostic stubs on `__ANDROID__ \|\| __WASM_PORT__ \|\| _WIN32` (`mainmenu_server.cpp:80-83`, `:173-174`) and `portInUse` returns `false` unconditionally on `_WIN32` (`:52-53`); where it does run, `system()`'s return value is discarded so a missing or failing `pkill` is indistinguishable from success, `pkill` is not a POSIX-mandated utility, and the filter is by process name only with no ownership or parentage test | [Local server control](subsystems/04-lobby-settings-input.md#local-server-control), [Task 8 extensions](subsystems/06-platform-ports.md#confirmed-findings) |
| BUG-034 | confirmed | High | High | Task 6 (audio-side lifetime closed by Task 7 cross-link) | `FrozenBubble`'s raw members have no initializers, so the constructor's early-return paths leave them indeterminate and `RunForEver`/`~FrozenBubble` then dereference them; Task 7 verified a *constructed* `AudioMixer` is failure-safe, so the defect is entirely the construction path | [Full-client runs](subsystems/04-lobby-settings-input.md#dynamic-evidence), [Task 7 cross-link](subsystems/05-render-audio.md#confirmed-findings) |
| BUG-035 | confirmed | Medium | High | Task 6 | Controller slots are never released and the slot index is unbounded: from slot 6 in-game controller input silently stops, and from slot 11 the derived scancode passes SDL's 512-entry keyboard array | [Input bounds](subsystems/04-lobby-settings-input.md#keyboard-controller-and-mouse-bounds-step-4) |
| BUG-036 | confirmed | Medium | High | Task 6 | The 20-scancode stride per player is smaller than SDL3's 26 gamepad buttons, so a button index of 20 or more aliases the next player's slot | [Input bounds](subsystems/04-lobby-settings-input.md#keyboard-controller-and-mouse-bounds-step-4) |
| BUG-037 | confirmed | Medium | High | Task 6 | Room-scoped lobby state, including the nickname-keyed team-override map and the chat scan counters, is never reset on part/join, so a previous room's assignments reappear in the next room | [Transition map](subsystems/04-lobby-settings-input.md#menu-and-room-state-transitions-step-1) |
| BUG-038 | confirmed | Low | High | Task 6 | On WASM the game list is rebuilt inside the WebSocket callback between frames while joining is an index into that list, so the join can target a room other than the highlighted one | [Transition map](subsystems/04-lobby-settings-input.md#menu-and-room-state-transitions-step-1) |
| BUG-039 | confirmed | Low | High | Task 6 | Local multiplayer is structured and documented for five players, but the setup panel caps at four and key configuration cycles players 1-4, leaving player 5's bindings unreachable | [Input bounds](subsystems/04-lobby-settings-input.md#keyboard-controller-and-mouse-bounds-step-4) |
| BUG-040 | confirmed | Medium | High | Task 6 | Per-player room options are only expressible for slots 1-5, so 10- and 20-player rooms silently ignore host configuration for slots 6-20 | [Option table](subsystems/04-lobby-settings-input.md#option-serialization-and-validation-step-2) |
| SEC-007 | confirmed | High | High | Task 6 (cross-links SEC-003 and SEC-004) | A peer-supplied `PLAYERTEAM_Pn` is never clamped between the option parser and `SetupSettings`, and gameplay then indexes the five-element `kTeamColors` with `team - 1` | [Option table](subsystems/04-lobby-settings-input.md#option-serialization-and-validation-step-2) |
| IMP-012 | confirmed | Medium benefit / Low effort / Low risk | High | Task 6; extended in Task 7 Fix Round 1; platform-layer siblings registered separately as IMP-015 in Task 8 | Remove or wire up the menu's unreachable code: the two-player panel, the network setup panel, the `editor` button, the `LevelEditor`/`Netplay` states, `selectedGameIndex`, `controllerInputs[5]`, the diverted gamepad branch, the throwing static settings initializer, and (Task 7) the `FrozenBubble::menuText` `TTFText` member, which has zero references outside `frozenbubble.h:96` | [Reachability traces](subsystems/04-lobby-settings-input.md#static-review), [Task 7 confirmed findings](subsystems/05-render-audio.md#confirmed-findings) |
| BUG-041 | confirmed | Medium | High | Task 7 (trigger set corrected in Fix Round 1) | Every transition animation frame leaks one 640×480 texture: `synchro_after` drops the texture it creates (by-value parameter) and `TransitionManager::transitionTexture` is never assigned, costing ~40–50 MB per game start (`mainmenu.cpp:497`, inside `SetupNewGame`) and per round reload (`bubblegame.cpp:1012`) — the only two `DoSnipIn` producers; menu return is not a trigger (`QuitToTitle` clears `firstRenderDone` with no `DoSnipIn`); reproduced at exactly 1.2 MB/frame against production objects | [Task 7 dynamic evidence](subsystems/05-render-audio.md#dynamic-evidence) |
| BUG-042 | confirmed | Medium | High | Task 7 | Every `NewGame` reloads 394 penguin animation textures per player and each player's `hurryTexture` over the prior pointers with no destroy site anywhere in the tree, leaking the previous set on every match start | [Task 7 confirmed findings](subsystems/05-render-audio.md#confirmed-findings) |
| BUG-043 | confirmed | Medium | High | Task 7 Fix Round 1 | The multiplayer targeting indicator can never render: `BubbleGame::targetingText` (`bubblegame.h:535`) receives no `LoadFont` anywhere in the tree, so `UpdateText` always early-returns at `ttftext.cpp:48` before `outTexture` is created and before `coords.w/h` are written, and `bubblegame_render.cpp:957` then renders a null texture through an indeterminate rect; reproduced against the production `ttftext.cpp` object (`coords_w=coords_h=0xCDCDCDCD`, `SDL_RenderTexture` rejected) | [Task 7 confirmed findings](subsystems/05-render-audio.md#confirmed-findings) |
| BUG-044 | confirmed | Medium | High | Task 7 Fix Round 1 (same class as BUG-001) | `MainMenu` stores five `IMG_Load` results into `activeSPButtons[]` unchecked (`mainmenu.cpp:124`) and `SPPanelRender` dereferences them at `mainmenu_panels.cpp:198` (`activeSPButtons[0]->w`) and `:213` (into production `overlook_`, which reads `orig->format` at `shaderstuff.cpp:1485`), so a missing single-player button asset crashes the client when the panel opens; the `:213` half reproduced under UBSan/ASan | [Task 7 confirmed findings](subsystems/05-render-audio.md#confirmed-findings) |
| BUG-045 | confirmed | Medium | High | Task 7 Fix Round 2 (promoted instance of IMP-007) | Levelset highscore rows lose their rendered text when stored: `HighscoreData` holds a `TTFText` by value, and `TTFText`'s copy constructor (`ttftext.h:52`) copies nothing while its copy assignment (`ttftext.h:53`) is a no-op, so `levelsetScores.push_back(newEntry)` (`highscoremanager.cpp:172`) discards the font/texture `RefreshTextStatus` had just installed at `:171`; no `CreateLevelImages` follows (`bubblegame_state.cpp:418` refreshes *before* `:422` mutates), and `highscoremanager.cpp:339` then renders a null texture through a `{0,0,0,0}` rect — reproduced against the production `ttftext.cpp` object (`copy tex=0x0 coords={0,0,0,0}`, `SDL_RenderTexture` rejected) | [Task 7 confirmed findings](subsystems/05-render-audio.md#confirmed-findings) |
| IMP-013 | confirmed | Medium benefit / Low effort / Low risk | High | Task 7 (attribution corrected in Fix Round 1) | Fix the off-by-one clamp bounds guarding the pixel helpers: `get_pixel`'s own `CLAMP(x,0,s->w)`/`CLAMP(y,0,s->h)` (`shaderstuff.cpp:49`) and the identical caller-side clamps that feed `set_pixel` at `shaderstuff.cpp:488` and `:1155` all admit one-past-the-end indices — `set_pixel` itself (`shaderstuff.cpp:41-45`) has no clamp at all. ASan-proven read on the production object; unreachable with shipped assets but a latent write via `points_` if the mask asset changes. Also assert the tight-pitch/32-bpp surface contract and bound `myLockSurface`'s retry loop | [Task 7 dynamic evidence](subsystems/05-render-audio.md#dynamic-evidence) |
| BUG-046 | confirmed | Medium | High | Task 8 (second consequence added in Task 8 Fix Round 1) | `AssetExtractor.extractFile` skips any destination that already exists with non-zero length (`AssetExtractor.java:106`) with no content, size, or timestamp comparison, and `extractAll` writes the `.assets_version` marker unconditionally (`:68-74`) after `extractDir`/`extractFile` have swallowed every exception into a `Log.e` (`:98-100`, `:115-117`). Two consequences of that one line: (1) a truncated-but-non-empty file from an interrupted extraction survives every later launch through the early return at `:53-61`, and **a version bump does not repair it** — `extractAll` re-enters `extractDir`, `:106` skips the file again, and `:68-74` rewrites the marker; only clearing app data or uninstalling repairs it; (2) an app update never refreshes an asset whose content changed while keeping its path, since only absent-or-empty paths are written — the error-free upgrade path silently ships the previous release's bytes. Android is also the one platform where `VerifyAssetDirectory` is compiled out (`frozenbubble.cpp:102`), so nothing checks the tree before use | [Task 8 confirmed findings](subsystems/06-platform-ports.md#confirmed-findings) |
| BUG-047 | confirmed | Low | High | Task 8 | The log file path is CWD-relative and its name counts launches rather than players: `frozenbubble.cpp:82-97` picks among `creator`/`joiner1`-`joiner4` by `stat`ing them in the process CWD, so six consecutive launches in one directory produced those five files and then reused `joiner4`; `Logger::Initialize`'s failure return is discarded at `:99`, and on a read-only CWD (a Finder-launched macOS `.app` and an Android process both have CWD `/`) `logger.cpp:97-100` returns before `SDL_SetLogOutputFunction`, so all file diagnostics vanish silently — both halves reproduced against the production `logger.cpp` object | [Task 8 dynamic evidence](subsystems/06-platform-ports.md#dynamic-evidence) |
| BUG-048 | confirmed | Medium | High | Task 8 | WASM settings and highscores are written to volatile memory: `SDL_GetPrefPath` on Emscripten returns `/libsdl/<org>/<app>/` created with plain `mkdir` (SDL 3.4.4 `src/filesystem/emscripten/SDL_sysfilesystem.c:40-79`), i.e. MEMFS, and occurrence counts measured on the linked artifact (`grep -o … \| wc -l`) are `IDBFS` **0**, `syncfs` **4** — all four inside `FS.syncfs`'s own definition, so no call site outside it exists — and `localStorage` **4**, all `fb_nickname` `ASM_CONSTS` entries: one `getItem` (`mainmenu.cpp:163`) and three `setItem` (`mainmenu.cpp:264`, `mainmenu_netpanel.cpp:80`, `mainmenu_input.cpp:1507`), the three writes being what create the persistence. Key bindings, sound/graphics settings, speed multiplier, mouse mode and both highscore files reset on every page load; only the nickname persists, and `~FrozenBubble`'s `Dispose` saves never run because `RunForEver` returns at `frozenbubble.cpp:238-239` | [Task 8 confirmed findings](subsystems/06-platform-ports.md#confirmed-findings) |
| REL-005 | confirmed | Medium | High | Task 8; remediation in Task 9 | 97 tracked entries under `android/app/jni/include/SDL2/` are git mode `120000` symlinks into four directories beneath the absolute private path `/Users/dericchau/ai/fb2-port/frozen-bubble-sdl2/android/app/jni/`; all 97 dangle in every clone, no build file/source/CI step references `jni/include`, and they are superseded by the four SDL3 submodules | [Task 8 confirmed findings](subsystems/06-platform-ports.md#confirmed-findings) |
| REL-006 | confirmed | Medium | High | Tasks 8 and 9 (Nix slice added in Task 9) | Stale platform build files and self-contradicting port documentation: `CMakeListsEmscripten.txt` lists 15 sources where the effective set is 28 (omitting 14 whose definitions the included TUs call) and selects SDL2 ports; `WASM_PORT.md` claims SDL2 ports, documents an `ASSET()` body that does not match `platform.h:34-45`, misattributes the `__WASM_PORT__` definition, and requires a websockify proxy that `web/README.md:17` says is unnecessary; `android/SETUP.md` documents prebuilt SDL2 libraries and SDL2 Java glue instead of the SDL3 submodules; `web/index.html`'s sole external script tag (`:136`, alongside an inline `Module` setup block at `:89-135`) loads a `frozen-bubble-sdl2.js` no build emits. Task 9 added the Nix slice: `default.nix` cannot build for three independent reasons — `buildInputs` names `SDL2`/`SDL2.dev`/`SDL2_ttf`/`SDL2_image`/`SDL2_mixer` (`:28-43`) against `CMakeLists.txt:18-21`'s `find_package(SDL3 REQUIRED)`; the install phase copies `build/frozen-bubble-sdl2` (`:57`), a name no target emits; and it copies `../share` (`:56`) from outside the unpacked source root — while `default.nix:21-22,62` and `flake.nix:3` still identify the project as the SDL2 port and `.gitignore:8` still ignores `/frozen-bubble-sdl2`. `shell.nix` and `flake.nix` wrap `default.nix` and inherit the breakage | [Task 8 confirmed findings](subsystems/06-platform-ports.md#confirmed-findings), [Task 9 build parity](subsystems/07-build-release-tooling.md#build-definition-parity-step-1) |
| REL-007 | confirmed | Medium | High | Tasks 8 and 9 (evidence and reachability added in Task 9) | Release APK signing and build-input integrity: a local `assembleRelease` emits `app-release-unsigned.apk` because the release build type declares no `signingConfig` — Task 9's read-only `:app:signingReport` states it directly, `Variant: release / Config: null / Store: null / Alias: null`; CI generates a fresh keystore per run with a literal password appearing four times in the public workflow (`.github/workflows/build.yml:390-398`), so consecutive releases carry different certificates and Android cannot upgrade one over another; and `gradle-wrapper.properties` pins `gradle-8.2-all.zip` with no `distributionSha256Sum`. Task 9 corrected the reachability: the Android job is **not** disabled at the pinned baseline, so the throwaway keystore reaches every tagged release, and it compounds with REL-004's literal `versionCode 10` to give two independent reasons a shipped APK cannot upgrade another | [Task 8 confirmed findings](subsystems/06-platform-ports.md#confirmed-findings), [Task 9 signing trace](subsystems/07-build-release-tooling.md#release-version-signing-and-artifact-flow-step-3) |
| REL-008 | confirmed | Medium | High | Task 8 (severity reassessed High → Medium in Task 8 Fix Round 1, where the registry's bare `High` and the notebook's "High for the macOS `make install` path; Medium overall" were reconciled); remediation in Task 9 | An installed macOS build resolves assets to the build machine's source tree: `CMakeLists.txt:223-226` computes `INSTALLED_ASSET_PATH` but nothing uses it, `DATA_DIR` comes from `ASSET_PATH` (default `${CMAKE_SOURCE_DIR}/share`, `:127-128`/`:140`), and `platform.cpp:109-121` recovers a prefix only for `.app` bundles — reproduced: a harness staged at `…/usr/local/bin/` resolved `g_dataDir` to the baked `/Users/dchau/gr/frozen-bubble-sdl3/share` and ignored the staged assets, which off the build machine reaches BUG-034 through the failing `VerifyAssetDirectory`. **No shipped artifact is affected**: the macOS release is a hand-built `.app` (`build.yml:155-163` copies `share` into `Contents/Resources/`, the one layout `platform.cpp:109-121` handles), the AppImage installs to `AppDir/usr/bin` with assets at `usr/share/frozen-bubble` (`:75-81`, recovered by the `/proc/self/exe` heuristic), and the Windows package copies `share` beside the `.exe` (`:255-260`). `default.nix:50` passes `-DASSET_PATH="$out/share"`, but Task 9 qualified that mitigation: the flag is correct in isolation while the derivation containing it cannot build (REL-006 extension), so it mitigates nothing for any user | [Task 8 dynamic evidence](subsystems/06-platform-ports.md#dynamic-evidence), [Task 9 build parity](subsystems/07-build-release-tooling.md#build-definition-parity-step-1) |
| IMP-014 | confirmed | Medium benefit / Low effort / Low risk | High | Task 8 | Exclude the duplicate and unused native libraries from the release APK: `libpng.so` is byte-identical to `libpng16.so` and shares its `SONAME`, so it can never be the file `SDL3_image` `dlopen`s, and `libvorbisenc.so` is named by no `DT_NEEDED` entry and no `dlopen` string — 819,904 + 1,846,824 = 2,666,728 bytes uncompressed across the three ABIs of a 37,290,226-byte APK | [Task 8 dynamic evidence](subsystems/06-platform-ports.md#dynamic-evidence) |
| REL-009 | confirmed | Low | High | Task 9 | Maintained documentation contradicts the shipped system. (a) `CLAUDE.md`'s CI section states the Linux/macOS/Windows/Android build and deploy jobs are "currently disabled with `if: false`" and that `release` "needs/packages only the WASM zip"; measured at the pinned baseline, **0** of **11** jobs carry `if: false`, `release` `needs` all five build jobs and attaches **5** files, and `CHANGELOG.md`'s `v2.4.27` entry states the restoration in the opposite words. The cost is not cosmetic — it caused this audit's own Task 8 to under-weight four shipped platforms. (b) `README.md:274-279` documents the public-server-list format as `myserver.example.com 1511`, but the GitHub list is parsed with `originalFormat=false` (`src/networkclient.cpp:1802`), which splits the first token on its last colon and treats the rest of the line as a display name: the documented form yields `port` left at the default 1511 and the display name `"1511"`, and any other port is silently ignored. `SetupServer.md:189-192` documents the correct `host:port  Name` form, so the two user-facing documents disagree | [Task 9 documentation review](subsystems/07-build-release-tooling.md#operational-documentation-against-actual-commands-step-6) |
| REL-010 | confirmed | Medium | High | Task 9 | The documented TLS certificate workflow discards the operator's real certificate. `docker/setup.sh:23`'s `key_ok` is `openssl rsa -in "$KEY" -check -noout`, which accepts only RSA keys, while `SetupServer.md:108-109` instructs the operator to run certbot — ECDSA by default since certbot 2.0. Reproduced: `openssl rsa -check -noout` on a `prime256v1` key exits **1** under LibreSSL 3.3 (`expecting an rsa key`) and under OpenSSL 3.6.3 (`Not an RSA key`). The `else` branch at `:37-40` then runs `openssl req -x509 -newkey rsa:2048 -keyout "$KEY" -out "$CERT"`, truncating **both** files the operator copied in at `SetupServer.md:123-124`; the stack comes up on a self-signed `CN=localhost` certificate that every browser/WASM client rejects, with no message that a valid certificate was destroyed. Medium rather than High because `/etc/letsencrypt` retains the originals. Compounding: `SetupServer.md:166-176`'s renewal block runs `cd docker` and then `sudo cp … docker/ssl/…`, a path that does not exist from inside `docker/`, so both copies fail and `./setup.sh -d` restarts on the unchanged certificate — which `cert_ok` accepts even when expired, since it checks parseability only | [Task 9 documentation review](subsystems/07-build-release-tooling.md#operational-documentation-against-actual-commands-step-6) |
| REL-011 | confirmed | Medium | High | Task 9 | Build inputs are unpinned and the SDL3 versions diverge across platforms. Measured on `.github/workflows/build.yml`: **27** `uses:` references, **0** commit-pinned, **5** pinned to the mutable branch `@master` — all five `josephbmanley/butler-publish-itchio-action`, each receiving `secrets.BUTLER_CREDENTIALS`, so a force-push or takeover on that repository's default branch runs attacker-chosen code in a job holding the itch.io deploy credential. Also unpinned: `mymindstorm/setup-emsdk@v14` with `version: 'latest'`, `linuxdeploy`'s rolling `continuous` release, `choco install nsis`, the macOS `brew install sdl3 …`, the four SDL3 MinGW archives (downloaded with no checksum), and the mutable `ubuntu:22.04`/`nginx:alpine` image tags. Measured consequence: only Linux, Windows and Android agree (SDL3 3.4.4 / image 3.4.2 / mixer 3.2.0 / ttf 3.2.2); WASM ships SDL3 **3.4.2** and SDL3_image **3.2.4** — the latter two minor families behind, pinned by `tools/ports/sdl3_image.py:10` — and macOS ships whatever Homebrew has (3.4.10 on the audit host). Same job, same cause: all three `sed` edits that install the port files silently no-op if upstream renames `SDL2_MIXER_FORMATS` in `src/settings.js`, `'SDL2_MIXER_FORMATS'` in `tools/settings.py`, or `diagnostics.warning('experimental'` in `tools/ports/sdl3.py`, and `latest` guarantees upstream will change under it | [Task 9 pinning review](subsystems/07-build-release-tooling.md#dependency-and-action-pinning-step-2) |
| REL-012 | confirmed | Medium | High | Task 9 | The macOS DMG is single-architecture and nothing says so. Neither `CMakeLists.txt` nor `.github/workflows/build.yml` contains any `CMAKE_OSX_ARCHITECTURES`, `-arch`, or universal-binary setting (measured: zero occurrences), so `build-macos` compiles for the `macos-latest` runner's own architecture and `dylibbundler` (`:163-168`) bundles Homebrew dylibs of that same architecture. The result ships as `frozen-bubble-macos.dmg` and to the itch.io channel `osx`, neither of which names an architecture, while `README.md:6` advertises plain "macOS". Macs of the other architecture download an app that cannot launch. The Linux job by contrast names `frozen-bubble-linux-x86_64.AppImage` correctly. Which architecture is excluded was not determined, because the audit executed no workflow | [Task 9 artifact flow](subsystems/07-build-release-tooling.md#release-version-signing-and-artifact-flow-step-3) |
| REL-013 | confirmed | Medium | High | Task 9 | Two CI steps cannot signal failure or cannot take effect. (a) The Windows packaging loop (`:261-269`) copies **20** named DLLs with `cp /mingw64/bin/$dll pkg/ 2>/dev/null \|\| true`, so a missing load-time requirement — `SDL3.dll`, `libstdc++-6.dll`, `libwinpthread-1.dll` are not optional — still yields a green job, a packaged NSIS installer, a GitHub release asset and an itch.io push, with the first symptom a loader error on a user's machine; the job runs no dependency walk and no smoke launch. (b) `Cache NDK` (`:342-346`) sets `path: ${{ env.ANDROID_SDK_ROOT }}/ndk/25.2.9519653`, but the `env` context is populated only from workflow/job/step `env:` maps and the workflow's only `env:` block defines `FORCE_JAVASCRIPT_ACTIONS_TO_NODE24` alone, so the expression interpolates empty and the path becomes the absolute `/ndk/25.2.9519653`; the step immediately after reads the same variable correctly as `$ANDROID_SDK_ROOT` in a `run:` shell. The NDK cache never hits and every Android run re-downloads it; no artifact is affected | [Task 9 artifact flow](subsystems/07-build-release-tooling.md#release-version-signing-and-artifact-flow-step-3) |
| REL-014 | confirmed | Medium | High | Task 9 | The vendored iniparser ships in every artifact with no licence, version, or provenance. `third_party/iniparser/` holds exactly four files — `iniparser.c`, `iniparser.h`, `dictionary.c`, `dictionary.h` — and no `LICENSE`, `COPYING`, `README`, or version marker; the two headers carry only `@file`/`@author N. Devillard`/`@brief` blocks with no copyright or licence text. The repository's single licence file, `COPYING`, does not mention iniparser, and `android/app/jni/iniparser/` is a second copy with the same gap. Upstream iniparser is MIT-licensed, whose one condition is that the copyright notice accompany all copies, and every shipped artifact links this code statically (`CMakeLists.txt:27-33` → `iniparser-static`; `android/app/CMakeLists.txt:60-64` → `iniparser`). The same gap blocks version tracking: the API shape (`const char * iniparser_getstring`, `iniparser_getint64`) places it in the 4.x family, but no release is recorded, so no upstream fix can be matched against it | [Task 9 IMP-008 closure](subsystems/07-build-release-tooling.md#imp-008-closure) |
| IMP-015 | confirmed | Medium benefit / Low effort / Low risk | High | Task 8 (extends IMP-012's unreachable-code class) | Remove the dead platform layer: `platform.cpp:38-52`'s `ASSET_FILES[]`/`extractAssets()` placeholder (never called; `AssetExtractor.java` does the work), `logger.cpp:24-27`'s unused `_WIN32` `mkdir` macro and `<direct.h>`, `shaderstuff.h:24-26`/`:28`'s `bzero` macro and `<iconv.h>` include that exist only for the block-commented `shaderstuff.cpp:1332-1360`, `AdsManager.init()` with zero callers, and `__ANDROID_PORT__` (one definition in a branch the shipping Android build never evaluates, one consumer whose `__ANDROID__` disjunct already selects correctly) | [Task 8 confirmed findings](subsystems/06-platform-ports.md#confirmed-findings) |
| IMP-016 | confirmed | High benefit / Low effort / Low risk | High | Task 9 | Run the registered checks in CI. Measured: **0** occurrences of `ctest`, `BUILD_TESTING`, `--target test`, `gradlew test` or `pytest` across `.github/workflows/build.yml`, the only file in `.github/workflows/`, so compilation is the entire automated gate for **11** jobs while **5** CTest tests sit registered and unrun. Add a `test` job (Ubuntu 22.04 Debug configure reusing the existing SDL3 build step, `ctest --output-on-failure`, all 5 passing once REL-002's isolation lands; macOS second) and make `release` `needs` it. Add to the same job the repository-hygiene assertions this audit had to make by hand: no tracked dangling symlink (REL-005), one version value across `server/CMakeLists.txt`, `src/platform.h`, `android/app/build.gradle` and the tag (REL-004), every declared build file configures (REL-006), a licence file per vendored dependency (REL-014), every `uses:` commit-pinned (REL-011), and no step that cannot fail (REL-013) | [Task 9 test-coverage map](subsystems/07-build-release-tooling.md#test-coverage-against-discovered-risks-step-5) |
| IMP-017 | confirmed | High benefit / Medium effort / Low risk | High | Task 9 | Settings, persistence, and input-bounds regression tests. Location `tests/gamesettings_test.cpp` linking `src/gamesettings.cpp` (Task 6's harness pattern). Inputs: isolated `CFFIXED_USER_HOME`; read-only preferences directory; INI with a syntax error; INI with an over-long line; `Key=99999`; `WindowHeight=100000`; `SpeedMultiplier=nan`; truncated highscore file; non-numeric level-history field. Assertions: `ReadSettings` terminates in bounded time (BUG-026); a syntax error preserves other stored keys (BUG-027); every scancode reaching `PlayerKeys` is `< SDL_SCANCODE_COUNT` (BUG-028); `WindowHeight` is clamped by the height bound (BUG-029); `SpeedMultiplier` is finite (BUG-030); a write failure is reported on a category SDL does not suppress (BUG-031); a corrupt highscore file leaves the object constructed (BUG-032, BUG-034). Extend with synthesized `SDL_Event` controller hot-plug cycles for slot release and the 20-vs-26 stride (BUG-035, BUG-036), the player-count reachability limits (BUG-039, BUG-040), and a seam over the process-control call so BUG-033 is assertable without running `pkill`. Matrix: Linux and macOS | [Task 9 test-coverage map](subsystems/07-build-release-tooling.md#test-coverage-against-discovered-risks-step-5) |
| IMP-018 | confirmed | High benefit / Medium effort / Medium risk | High | Task 9 | Promote Task 5's production-object gameplay harness into CTest — it already reproduced BUG-018, BUG-019 and BUG-025 against unchanged production objects and is simply unregistered. Location `tests/bubblegame_rules_test.cpp` plus the existing test-TU visibility seam. Inputs: player counts 1/2/5/6/20; team counts 1-5; colours 5 and 8; both grid orientations; three consecutive rounds; Clear Mode; a simultaneous final loss; a quit-then-new-match transition that shrinks the player count; `deltaScale = 15`. Assertions: Clear Mode honoured on board clear (BUG-018); simultaneous final loss resolves as a draw (BUG-019); no malus survives a match transition into a cleared board (BUG-020); departures respect configured continuation and victory limits (BUG-021, BUG-023); chain targeting uses flipped-grid parity (BUG-022); clear-win accounting is independent of `F`/stick ordering (BUG-024); a maximum-delta step cannot pass through an occupied cell (BUG-025). Matrix: Linux and macOS, normal and ASan+UBSan | [Task 9 test-coverage map](subsystems/07-build-release-tooling.md#test-coverage-against-discovered-risks-step-5) |
| IMP-019 | confirmed | High benefit / High effort / Low risk | High | Task 9 | Protocol and parser unit tests for the server and network client. Location `tests/server_parse_test.c` (linking `server/net.c`, `server/game.c`, `server/tools.c` with a test `main`) and `tests/netclient_parse_test.cpp`. Inputs are well-formed and boundary-shaped only — hostile traffic stays outside the audit's scope: a WebSocket handshake split across two buffers (BUG-006); a short send return (BUG-007); an empty `CREATE ` nickname (BUG-009); a room reaching one player then zero (BUG-005); `START` repeated while peers are priority (BUG-011); an upload exceeding the admission limit (BUG-004); a room kick and a post-start close (BUG-003); `Content-Length` at `INT_MAX` (SEC-002); a 128-byte LAN probe with no NUL (SEC-005); a 40-digit numeric field (SEC-006); a frame whose claimed sender is not the connection's seat (SEC-004); a `setgid` stub returning failure (SEC-001); an off-board coordinate and a `PLAYERTEAM_Pn` outside 1-5 (SEC-003, SEC-007); an out-of-order lobby response and an ordinary rejection type (BUG-015); a writable `connect` with `SO_ERROR` set (BUG-016); a greeting split across `recv` calls (BUG-017); a round-2 sync arriving before `SyncNetworkLevel` (BUG-014); a part/rejoin cycle (BUG-013, BUG-037); a game-list rebuild between highlight and join (BUG-038); a `%zd`-formatted `size_t` (REL-001). Assertions: each parse either succeeds with the correct value or is rejected without indexing outside its buffer, and no path calls `exit()`. Matrix: Linux; macOS for the client half | [Task 9 test-coverage map](subsystems/07-build-release-tooling.md#test-coverage-against-discovered-risks-step-5) |
| IMP-020 | confirmed | High benefit / Medium effort / Low risk | High | Task 9 | Packaged-artifact smoke tests — every finding in this class was established statically or at harness level because no gate could launch a packaged artifact. Location: a `package-smoke` job per platform after each build job. Inputs: the produced AppImage, `.app`, installer directory, APK and WASM bundle, on a machine with no source tree at the baked `DATA_DIR`. Assertions: the binary starts under dummy video/audio drivers and exits cleanly (REL-003, REL-008, BUG-034); `g_dataDir` resolves inside the package rather than a build path (REL-008); the log file is created outside the process CWD and initialization failure is reported (BUG-047); on Android an emulator install-over-install refreshes a changed asset and repairs a truncated one (BUG-046); on WASM a headless browser reload preserves settings and highscores (BUG-048); a dependency walk finds no unresolved import (REL-013); the APK is signed by a stable secret-held certificate whose `versionCode` exceeds the previous release's (REL-007, REL-004); the DMG contains the expected architectures (REL-012); and `docker/setup.sh` leaves a pre-existing ECDSA key byte-identical (REL-010). Matrix: all five platforms | [Task 9 test-coverage map](subsystems/07-build-release-tooling.md#test-coverage-against-discovered-risks-step-5) |
| IMP-021 | confirmed | Medium benefit / Medium effort / Low risk | High | Task 9 | Resource-lifetime regression job on the one platform where LeakSanitizer works. Apple ASan cannot detect leaks, so every leak conclusion in this audit rests on ownership tables and RSS measurement. Location: a Linux-only CI job built with `-fsanitize=address,undefined` and `ASAN_OPTIONS=detect_leaks=1`, running the IMP-018 harness plus a transition and text-lifecycle driver. Inputs: a game start; a round reload; ten transition animations covering all five effect families with a seeded RNG; a levelset highscore insert; a missing single-player button asset; a missing candy asset. Assertions: no leak across repeated game starts (BUG-041, BUG-042); a missing asset yields a diagnostic and clean exit rather than a null dereference (BUG-001, BUG-044); a stored `TTFText` retains its texture (BUG-045); the targeting indicator has a font before it is rendered (BUG-043); the pixel helpers reject one-past-the-end indices (IMP-013). Matrix: Linux only | [Task 9 test-coverage map](subsystems/07-build-release-tooling.md#test-coverage-against-discovered-risks-step-5) |
| IMP-022 | confirmed | Medium benefit / Low effort / Low risk | High | Task 9 | Unify the warning configuration across build definitions. `CMakeLists.txt:14` applies `-Wall -Wextra -pedantic -Wno-pointer-arith` to the native, Windows and WASM builds and, by directory scope, to `server/` as well, while `android/app/CMakeLists.txt` applies **none** — so the NDK toolchain compiles the same 28 translation units, including the 32-bit `armeabi-v7a` ABI where narrowing and pointer-width diagnostics are most informative, with warnings off. The `docker/Dockerfile` server build also loses `-Wno-pointer-arith` because it configures `server/` as a top-level project. Lift the flag set into a shared interface target or a small included `.cmake` module and consume it from all three roots | [Task 9 build parity](subsystems/07-build-release-tooling.md#build-definition-parity-step-1) |
| IMP-023 | confirmed | Medium benefit / Low effort / Low risk | High | Task 9 | Constrain the iniparser dependency boundary. `CMakeLists.txt:24`'s `find_package(iniparser QUIET COMPONENTS static)` silently prefers any system iniparser over the bundled copy; `cmake/Findiniparser.cmake` declares no `iniparser_VERSION` and passes no `VERSION_VAR` to `find_package_handle_standard_args` (`:51-55`); and the bundled sources carry no version marker — so the parser implementation the build actually uses is a property of the host, not the repository. `README.md:176` states "`iniparser` is bundled — no separate install needed", which holds only when no system copy happens to be installed. Either pass a minimum version and match it against the vendored API level, or drop the `find_package` call and always build the bundled sources; record the vendored release alongside the licence text REL-014 requires. The fallback guard itself is sound and was dismissed as a candidate | [Task 9 IMP-008 closure](subsystems/07-build-release-tooling.md#imp-008-closure) |

Task 6 added BUG-026 through BUG-040, SEC-007, and IMP-012, and resolved the
menu/settings side of BUG-021, BUG-023, SEC-004, and IMP-005 by extending those
entries instead of allocating new IDs. Six Task 6 defects (BUG-026, BUG-027,
BUG-028, BUG-029, BUG-030, BUG-031 plus BUG-032 and BUG-034) were reproduced at
runtime against unchanged production code under an isolated preference home;
the remainder are complete source traces. SEC-007 was not reproduced with
traffic, in line with the user's security-runtime restriction.

Task 7 confirmed the inherited BUG-001 (both `TextureEx` null-deref orderings
reproduced under UBSan against production code), added BUG-041 (transition
texture leak, reproduced at 1.2 MB/frame through linked production objects)
and BUG-042 (per-match penguin/hurry texture reload leak, complete static
causal proof), confirmed IMP-007 and IMP-010 as improvements with their
cross-owner dispositions finished, added IMP-013 (ASan-demonstrated
pixel-helper bounds defect, unreachable with shipped assets), and closed the
render slices of IMP-006/IMP-008 without promotion.

Task 7 Fix Round 1 (independent review of commit `d9597304`) corrected four
overstated or contradicted facts in that gate — BUG-001's leak quantity (2
leaked rects per `InitCandy`, not 6, because the call sites are an
`if`/`else if` chain), BUG-041's trigger set (game start and round reload only;
menu return is not a `DoSnipIn` producer), the pixel-format claim (seven of
eight probed files are 4 bpp ABGR8888; `back_one_player.png` is RGB24 at 3 bpp
and is blit-only), and IMP-013's attribution (`set_pixel` has no clamp; the
off-by-one clamps are `get_pixel`'s own and the call sites at
`shaderstuff.cpp:488`/`:1155`). It also reopened IMP-005's render-slice closure
and promoted the reachable instance to **BUG-043**, registered **BUG-044** from
an unchecked-load path the `mainmenu_panels.cpp` coverage row had passed, and
extended IMP-012 with the dead `FrozenBubble::menuText` member. Both new
defects were reproduced against unchanged production objects.

Task 7 Fix Round 2 (second independent review, of commit `f4d44bda`) confirmed
those five corrections but found that Fix Round 1 had itself introduced a false
quantity: the ownership table's `TTFText` row read `bubblegame.h:534`'s stale
"3-5 player mode" comment instead of the array bound and reported 5 instances
where `MAX_NET_PLAYERS` (`bubblegame.h:251`) makes 20, totalling 23 where the
declarations give **38** fixed members plus a runtime-variable per-highscore
set. That correction leaves the two-never-font-loaded conclusion intact for the
array, but separating out the per-highscore `layoutText` exposed **BUG-045**.
Two further declaration-derived quantities were corrected in the same sweep
(the `MainMenu` texture family, "~44" → 37; `RenderRoundStats`'s churn, "~30
`cell()` calls" → 24 static call sites). The incomplete `idleSPButtons`
dismissal was completed with a full consequence trace, and the mechanism
proposed for it was **disproved**: `SDL_GetTextureSize` zeroes its outputs
before rejecting a null texture in both the pinned SDL 3.4.4 source and the
linked 3.4.10 runtime, so the rect is deterministic zeros and nothing draws —
it remains a dismissal, not a defect. At the close of Task 7 the registry held
69 unique IDs: BUG-001..045, SEC-001..007, REL-001..004, IMP-001..013.

Task 8 added BUG-046, BUG-047, BUG-048, REL-005, REL-006, REL-007, REL-008,
IMP-014, and IMP-015, and resolved the platform side of REL-003 (the `server/`
subdirectory is excluded from the build on `WIN32 OR MINGW OR EMSCRIPTEN`, so
this project cannot build the Windows server that `server/win32_compat.h`
supports; only the Windows client ships), REL-004 (a third version string —
`platform.h:23`'s `APP_VERSION "v2.4.26"`, rendered at
`mainmenu_panels.cpp:471` — against the audited tag `v2.4.27` and the built
APK's `versionName 2.4.27`), and BUG-033 (reachable only on macOS and Linux
because `StartLocalServer` is a stub on `__ANDROID__ || __WASM_PORT__ ||
_WIN32`; `system()`'s return is discarded and `pkill` is not POSIX-mandated) by
extending those entries instead of allocating new IDs. BUG-047 and REL-008 were
reproduced against unchanged production `logger.cpp`/`platform.cpp` objects;
BUG-048, REL-005, REL-006, REL-007 and IMP-014 were established from the built
artifacts (a complete local WASM link and a three-ABI release APK); BUG-046 is a
complete code-supported chain with no device run. Six candidates were dismissed
with consequence-tracing counter-evidence, one of them — that
`cmake/Emscripten.cmake` breaks the documented configure — disproved by running
the command. At the close of Task 8 the registry held **78 unique IDs**:
BUG-001..048, SEC-001..007, REL-001..008, IMP-001..015.

Task 8 Fix Round 1 allocated **no new ID** and renumbered none. It corrected
BUG-048's `syncfs`/`localStorage` occurrence counts (stated as one each; both
are **four**, because the recorded evidence measured first-occurrence indices
rather than counts) and its causal statement (three `setItem` writes create the
persistence, not the one `getItem` read); extended **BUG-046** with the two
further consequences of the same `AssetExtractor.java:106` short-circuit — a
version bump does **not** repair a truncated file, and an app update never
refreshes an asset whose content changed — rather than allocating a new ID,
because the root cause and the fix are identical; and reassessed **REL-008**
from a bare `High` to **Medium**, reconciling the registry with the notebook's
"High for the macOS `make install` path; Medium overall" on the verified facts
that no shipped desktop layout takes the fall-through path and that
`default.nix:50` already passes `-DASSET_PATH`. A count sweep re-derived twenty
quantities across the four Task 8 documents with commands that measure the
claim; eighteen reproduced exactly and two were wrong (the counts above, and the
recursive submodule count — 37 recorded, **38** actual).

Task 9 added REL-009 through REL-014 and IMP-016 through IMP-023, and resolved
the packaging side of REL-004 (five distinct version strings, the literal
`versionCode 10` that no workflow step overrides, and the missing Windows
`VERSIONINFO`; severity reassessed Low → Medium because two tagged releases
cannot upgrade one another), REL-006 (the Nix slice: `default.nix` cannot build
for three independent reasons and still identifies the project as the SDL2
port), REL-007 (`:app:signingReport` evidence plus the corrected reachability),
and REL-008 (the `default.nix:50` mitigation qualified — the flag is right but
the derivation cannot complete) by extending those entries instead of allocating
new IDs. REL-002 keeps its Task 3 severity; Task 9 discharged its remediation
ownership as a design, changing no test file. **IMP-008 is closed**: neither
analyzer inspects any file type Task 9 owns, so zero IMP-008-family diagnostics
have a Task 9 path, and the vendored-dependency boundary Task 2 deferred
produced IMP-023 and REL-014 instead. Four candidates were dismissed with
consequence-tracing counter-evidence, one of them — that
`cmake_uninstall.cmake.in`'s `exec_program` is removed in CMake 4 — disproved by
running it (exit 0, `CMP0153` developer warning only). REL-010 is the one Task 9
defect with a reproduced predicate; the rest are complete code-supported chains
measured against the workflow, the build files, and the linked artifacts,
because the audit executed no workflow, started no container, and performed no
external network operation. The registry now holds **92 unique IDs**:
BUG-001..048, SEC-001..007, REL-001..014, IMP-001..023.

Task 9 also corrected a premise this audit had inherited from `CLAUDE.md`. That
file states the Linux, macOS, Windows and Android build jobs and their itch.io
deploy jobs are disabled with `if: false`, leaving WASM as the only platform CI
ships. Measured at the pinned baseline, **0** of **11** jobs carry `if: false`,
the `release` job `needs` all five build jobs and attaches **5** files, and
`CHANGELOG.md`'s `v2.4.27` entry records the restoration explicitly. Task 8's
statement that BUG-048 "fires on every page load of the WASM build — per
`CLAUDE.md` the only platform CI currently ships" is therefore wrong in its
clause about CI: BUG-048 still fires on every WASM page load, but WASM is one of
five platforms CI builds and releases, and BUG-046, REL-007 and REL-012 reach
shipped Android and macOS artifacts accordingly. The drift itself is registered
as REL-009.

Task 2 alone confirmed no defect. Tasks 3 through 5 promoted only findings with
complete source-level causal proof. Runtime/security reproduction was not
performed by user direction, so security findings are code-supported inferences
rather than observed runtime facts. Raw analyzer diagnostics were not
bulk-promoted. Task 4's required bot unit checks passed; its available harness
could not autonomously create, start, synchronize, and assert a two-round game.
Task 5's helper tests, pure-helper oracle, and headless production-object
boundary harness passed. Fix Round 1 also reproduced BUG-025 with the linked
production collision predicate, free-cell selector, and placement primitive;
no graphical or multi-client gameplay runtime was claimed.
