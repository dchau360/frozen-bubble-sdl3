# Replace this file with your real SSL certificate chain.
#
# Using Let's Encrypt (recommended):
#   sudo certbot certonly --standalone -d yourdomain.com
#   sudo cp /etc/letsencrypt/live/yourdomain.com/fullchain.pem docker/ssl/fullchain.pem
#
# Or run:  docker/setup.sh  to generate a self-signed cert for local testing.
# Self-signed certs will NOT work for browser (WASM) clients on public HTTPS pages.
